#pragma once
#include <unordered_map>
#include <vector>
#include <string>
#include <map>
#include "SQLite_unit_data.h"
#include "components.h"

namespace Load_Object_List {



  //std::unordered_map<std::string, std::vector<std::string>> gameObjects;

  // read the db and get the unit types, assign to vector
  void Read_DB() {
    /* Entity_Loader::Get_All_Of_Class(std::string entity_class) */
    
    /* std::string type; */
    /* std::string objectName; */
    /* gameObjects.at(type).emplace_back(objectName); */
  }









  
}

